`import makeSquares from '../../../utils/make-squares'`
`import { module, test } from 'qunit'`

module 'makeSquares'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = makeSquares()
  assert.ok result
