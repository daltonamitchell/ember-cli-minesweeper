`import plantMines from '../../../utils/plant-mines'`
`import { module, test } from 'qunit'`

module 'plantMines'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = plantMines()
  assert.ok result
